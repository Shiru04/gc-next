import { ServiceRequestForm } from "@/components/service/ServiceRequestForm";
import { buildMetadata } from "@/lib/seo";
export const metadata = buildMetadata({ title: "Request HVAC Service", description: "Request AC repair or HVAC tune-up service from GC Heating & Cooling.", path: "/request-service" });
export default async function RequestServicePage({ searchParams }: { searchParams: Promise<{ service?: string }> }) { const { service } = await searchParams; return <main className="mx-auto max-w-3xl px-4 py-16 sm:py-24"><p className="font-bold uppercase tracking-wide text-brand-red">GC Heating &amp; Cooling</p><h1 className="mt-3 text-4xl font-extrabold">Request Service</h1><p className="mt-4 max-w-2xl text-lg text-black/70">Tell us what you need and our team will help schedule the right service.</p><div className="mt-8 rounded-2xl border border-black/10 bg-brand-gray p-5 sm:p-8"><ServiceRequestForm initialService={service} /></div></main>; }
